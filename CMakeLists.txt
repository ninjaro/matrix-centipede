# This file is generated by `marx sync`. Do not edit by hand.

cmake_minimum_required(VERSION 3.20)

project(matrix_centipede VERSION 0.1.0 LANGUAGES C CXX)

set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)
set(CMAKE_EXPORT_COMPILE_COMMANDS ON)

include(GNUInstallDirs)

if (NOT CMAKE_BUILD_TYPE)
    set(CMAKE_BUILD_TYPE Debug CACHE STRING "Build type" FORCE)
endif ()

function(ecosystem_apply_defaults target_name)
    get_target_property(target_type ${target_name} TYPE)
    set(target_scope PRIVATE)
    if (target_type STREQUAL "INTERFACE_LIBRARY")
        set(target_scope INTERFACE)
    endif ()

    if (MSVC)
        target_compile_options(${target_name} ${target_scope} /W4 /WX)
    elseif (CMAKE_CXX_COMPILER_ID MATCHES "Clang|GNU")
        target_compile_options(${target_name}
                ${target_scope}
                -Wall
                -Wextra
                -Wpedantic
                -Wconversion
                -Wsign-conversion
                -Wshadow
                -Werror
                "$<$<COMPILE_LANGUAGE:CXX>:-Wnon-virtual-dtor>"
        )
    endif ()

    if (ECOSYSTEM_ENABLE_COVERAGE AND CMAKE_CXX_COMPILER_ID MATCHES "Clang")
        target_compile_options(${target_name}
                ${target_scope}
                -fprofile-instr-generate
                -fcoverage-mapping
        )
        target_link_options(${target_name}
                ${target_scope}
                -fprofile-instr-generate
                -fcoverage-mapping
        )
    endif ()
endfunction()
function(ecosystem_stage_assets target_name)
    add_custom_command(
            TARGET ${target_name} POST_BUILD
            COMMAND ${CMAKE_COMMAND} -E copy_directory
            ${CMAKE_CURRENT_SOURCE_DIR}/assets
            $<TARGET_FILE_DIR:${target_name}>/assets
    )
endfunction()



set(MATRIX_CENTIPEDE_MATRIX_CORE_MATRIX_HEADERS
        ${CMAKE_CURRENT_SOURCE_DIR}/core/include/core/dense_matrix.hpp
        ${CMAKE_CURRENT_SOURCE_DIR}/core/include/core/dense_matrix.tpp
)
set(MATRIX_CENTIPEDE_MATRIX_CORE_MATRIX_SOURCES
)
set(MATRIX_CENTIPEDE_MATRIX_CORE_MATRIX_RUNTIME_SOURCES
)
set(MATRIX_CENTIPEDE_MATRIX_API_API_HEADERS
        ${CMAKE_CURRENT_SOURCE_DIR}/core/include/api/dense_matrix_api.h
)
set(MATRIX_CENTIPEDE_MATRIX_API_API_SOURCES
        ${CMAKE_CURRENT_SOURCE_DIR}/core/src/api/dense_matrix_api.cpp
)
set(MATRIX_CENTIPEDE_MATRIX_API_API_RUNTIME_SOURCES
        ${CMAKE_CURRENT_SOURCE_DIR}/core/src/api/dense_matrix_api.cpp
)
add_library(matrix_core__matrix INTERFACE)

target_compile_features(matrix_core__matrix INTERFACE cxx_std_20)

target_include_directories(matrix_core__matrix INTERFACE
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/core/include>"
    "$<INSTALL_INTERFACE:${CMAKE_INSTALL_INCLUDEDIR}>"
)

ecosystem_apply_defaults(matrix_core__matrix)


add_library(matrix_api__api SHARED
    ${MATRIX_CENTIPEDE_MATRIX_API_API_HEADERS}
    ${MATRIX_CENTIPEDE_MATRIX_API_API_SOURCES}
)

target_compile_features(matrix_api__api PUBLIC cxx_std_20)

target_include_directories(matrix_api__api PUBLIC
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/core/include>"
    "$<INSTALL_INTERFACE:${CMAKE_INSTALL_INCLUDEDIR}>"
)

target_link_libraries(matrix_api__api PUBLIC
    matrix_core__matrix
)

set_target_properties(matrix_api__api PROPERTIES OUTPUT_NAME matrix)

ecosystem_apply_defaults(matrix_api__api)


# The visitor name is generated state; the selected target keeps its identity.
# Retire a previous link/copy before a new selection can build at the same path.
foreach(facade_name mvp mvp.exe)
    if(EXISTS "${CMAKE_CURRENT_BINARY_DIR}/CMakeFiles/manifesto-${facade_name}.owned")
        file(REMOVE "${CMAKE_CURRENT_BINARY_DIR}/${facade_name}"
                    "${CMAKE_CURRENT_BINARY_DIR}/CMakeFiles/manifesto-${facade_name}.owned")
    endif()
endforeach()

get_target_property(MANIFESTO_FACADE_TYPE matrix_api__api TYPE)
if(MANIFESTO_FACADE_TYPE STREQUAL "EXECUTABLE" AND NOT ANDROID)
    if(WIN32)
        set(MANIFESTO_FACADE_NAME mvp.exe)
    else()
        set(MANIFESTO_FACADE_NAME mvp)
    endif()
    file(GENERATE
        OUTPUT "${CMAKE_CURRENT_BINARY_DIR}/CMakeFiles/manifesto-facade-$<CONFIG>.cmake"
        CONTENT [=[
set(source "$<TARGET_FILE:matrix_api__api>")
set(destination "${FACADE_DIR}/${FACADE_NAME}")
if(source STREQUAL destination)
    return()
endif()
if(WIN32)
    file(CREATE_LINK "${source}" "${destination}" COPY_ON_ERROR)
else()
    file(RELATIVE_PATH relative "${FACADE_DIR}" "${source}")
    file(CREATE_LINK "${relative}" "${destination}" SYMBOLIC)
endif()
file(WRITE "${FACADE_DIR}/CMakeFiles/manifesto-${FACADE_NAME}.owned" "generated\n")
]=])
    add_custom_target(_manifesto_visitor_facade_matrix_centipede ALL
        COMMAND "${CMAKE_COMMAND}"
            "-DFACADE_DIR=${CMAKE_CURRENT_BINARY_DIR}"
            "-DFACADE_NAME=${MANIFESTO_FACADE_NAME}"
            -P "${CMAKE_CURRENT_BINARY_DIR}/CMakeFiles/manifesto-facade-$<CONFIG>.cmake"
        DEPENDS matrix_api__api
        VERBATIM)
    set_property(DIRECTORY APPEND PROPERTY ADDITIONAL_CLEAN_FILES
        "${CMAKE_CURRENT_BINARY_DIR}/${MANIFESTO_FACADE_NAME}"
        "${CMAKE_CURRENT_BINARY_DIR}/CMakeFiles/manifesto-${MANIFESTO_FACADE_NAME}.owned")
endif()

install(TARGETS matrix_core__matrix EXPORT matrix_centipedeTargets
        RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
        LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
        ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR}
)

install(FILES ${CMAKE_CURRENT_SOURCE_DIR}/core/include/core/dense_matrix.hpp DESTINATION "${CMAKE_INSTALL_INCLUDEDIR}/core")

install(FILES ${CMAKE_CURRENT_SOURCE_DIR}/core/include/core/dense_matrix.tpp DESTINATION "${CMAKE_INSTALL_INCLUDEDIR}/core")

install(TARGETS matrix_api__api EXPORT matrix_centipedeTargets
        RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
        LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
        ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR}
)

install(FILES ${CMAKE_CURRENT_SOURCE_DIR}/core/include/api/dense_matrix_api.h DESTINATION "${CMAKE_INSTALL_INCLUDEDIR}/api")

include(CMakePackageConfigHelpers)
set(_matrix_centipede_package_dir "${CMAKE_CURRENT_BINARY_DIR}/matrix_centipede-package")
file(MAKE_DIRECTORY "${_matrix_centipede_package_dir}")
file(WRITE "${_matrix_centipede_package_dir}/matrix_centipedeConfig.cmake.in" [==[
if (TARGET matrix_centipede::matrix_api__api)
    return()
endif ()
set(_matrix_centipede_saved_kde "${ECOSYSTEM_PROFILE_KDE}")
set(ECOSYSTEM_PROFILE_KDE "@ECOSYSTEM_PROFILE_KDE@")
set(ECOSYSTEM_PROFILE_KDE "${_matrix_centipede_saved_kde}")
unset(_matrix_centipede_saved_kde)

include("${CMAKE_CURRENT_LIST_DIR}/matrix_centipedeTargets.cmake")
]==])
configure_file("${_matrix_centipede_package_dir}/matrix_centipedeConfig.cmake.in"
               "${_matrix_centipede_package_dir}/matrix_centipedeConfig.cmake" @ONLY)
write_basic_package_version_file("${_matrix_centipede_package_dir}/matrix_centipedeConfigVersion.cmake"
                                VERSION ${PROJECT_VERSION} COMPATIBILITY SameMajorVersion)
install(EXPORT matrix_centipedeTargets NAMESPACE matrix_centipede::
        DESTINATION "${CMAKE_INSTALL_LIBDIR}/cmake/matrix_centipede")
install(FILES "${_matrix_centipede_package_dir}/matrix_centipedeConfig.cmake"
              "${_matrix_centipede_package_dir}/matrix_centipedeConfigVersion.cmake"
        DESTINATION "${CMAKE_INSTALL_LIBDIR}/cmake/matrix_centipede")

